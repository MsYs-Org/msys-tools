[project]
name = "msys-tools"
version = "0.1.35"
description = "Windows-to-Linux development tools for MSYS"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
msys-dev = "msys_tools.dev:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
